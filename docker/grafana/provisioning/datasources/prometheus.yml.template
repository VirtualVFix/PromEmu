apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "${GRAFANA_REFRESH_INTERVAL}"
      queryTimeout: "60s"
      httpMethod: "POST"
    secureJsonData: {}
